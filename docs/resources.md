# Resource reference — `agent/v1`

Auto-generated by `agent-blueprint docs` from the harness `scheme`.
9 kinds registered under `agent/v1`.

## Table of contents

- [`Agent`](#agent) — Persona + behavior racine ; délègue à des postures.
- [`AzureFoundryModel`](#azurefoundrymodel) — Service de complétion Azure AI Foundry (clé API ou Entra ID).
- [`McpStdio`](#mcpstdio) — Source d'outils MCP via transport stdio ; connexion persistante.
- [`Memory`](#memory) — Tooling de mémorisation volatile par AgentContext.
- [`OllamaModel`](#ollamamodel) — Service de complétion via un daemon Ollama (OpenAI-compatible).
- [`OpenAIModel`](#openaimodel) — Service de complétion OpenAI (clé inline ou `OPENAI_*`).
- [`Posture`](#posture) — État actif avec instruction + tooling + hooks ; activable via une route.
- [`Preset`](#preset) — Conteneur de mutualisation ; inert tant qu'il n'est pas étendu via `extends`.
- [`Skill`](#skill) — Bundle toggable (instruction + tooling + hooks) attaché au runtime.

## Shared `spec` shapes

The following sub-schemas are carried by every kind that exposes `spec.tooling`,
`spec.hooks` or `spec.guardrails` (Agent, Posture, Skill, Preset). The full
conceptual reference lives in `docs/resources.spec.md`.

### `spec.tooling[*]` — discriminated union on `type`

```yaml
# Route — expose a posture transition as an LLM-callable tool.
- type: route
  name: <tool_name>
  posture: <target_posture>
  description: <intent>           # optional
  params: [...]                   # optional

# Toolset — select tools from named resources, or by label selector.
# `tools` and `selector` are mutually exclusive.
- type: toolset
  tools: ["<resource>/<tools>"]   # OR
  selector: { matchLabels: { ... } }

# Subagent — delegate to another agent.
- type: subagent
  agent_id: <agent_resource_name>
```

### `spec.hooks` — automations per trigger

```yaml
spec:
  hooks:
    on_start:      [{ type: tooluse, tool: <tool>, args: { ... } }, ...]
    on_completion: [...]
    on_tool_use:   [...]
    on_tool_error: [...]
```

Hook variants: `tooluse` (call a tool), `route` (activate a posture), `exit`.

### `spec.guardrails[*]` — assertions on tool calls

```yaml
spec:
  guardrails:
    - name: <unique_local_name>
      appliesTo: "*" | [<tool_names>] | { matchLabels: { ... } }
      assertion: <expression>       # optional
      message: <violation_message>  # optional
```

## Kinds

## `Agent`

> Persona + behavior racine ; délègue à des postures.

- **apiVersion:** `agent/v1`
- **runtime surface:** Permanent (`spec.tooling` + presets étendus)

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `extends` | string[] | no | — | Presets fusionnés au load (mécanique kind-spécifique). |
| `description` | string | no | — | Description humaine (intent exposé au LLM pour les skills). |
| `model` | string | yes | — | Nom de la ressource de modèle fournissant le `ThreadCompletionService`. |
| `initial_posture` | string | no | — | Posture active à l'init de l'AgentContext. |
| `instruction` | union: object \| object \| string | yes | — | Instruction (`{ $ref }`, `{ content }`, ou chemin de fichier). |
| `tooling` | union: object \| object \| object[] | no (default) | `[]` | Surface d'outils — entries `toolset` / `route` / `subagent`. |
| `max_tool_rounds` | integer | no | — | Limite de tours outil/complétion. |
| `hooks` | object | no | — | Automations par trigger (`on_start` / `on_completion` / `on_tool_use` / `on_tool_error`). |
| `guardrails` | object[] | no | — | Assertions appliquées sur les tool calls. |

### Example

```yaml
apiVersion: agent/v1
kind: Agent
metadata:
  name: pantri_chef
  labels: { app: cooking }
spec:
  model: model-default
  extends: [harness/conversational]
  initial_posture: greet
  instruction: { $ref: ./instructions/pantri.persona.md }
  tooling:
    - type: route
      name: greet
      posture: greet
```

### Notes

- Le tooling permanent de l'agent vit toute la durée du context.
- `extends: [harness/conversational]` est l'équipement transverse recommandé.

## `AzureFoundryModel`

> Service de complétion Azure AI Foundry (clé API ou Entra ID).

- **apiVersion:** `agent/v1`
- **runtime surface:** Aucune (service pur)

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `description` | string | no | — | Description humaine (intent exposé au LLM pour les skills). |
| `endpoint` | string | yes | — | Endpoint Foundry (OpenAI-compatible). |
| `deployment` | string | yes | — | Nom du deployment Foundry (passé comme `model`). |
| `auth` | enum: `"apiKey"` \| `"entraId"` | no (default) | `apiKey` | Mode d'authentification Foundry (`apiKey` ou `entraId`). |
| `apiKey` | string | no | — | Clé API (requis si `auth=apiKey`, défaut `AZURE_OPENAI_API_KEY`). |

### Example

```yaml
apiVersion: agent/v1
kind: AzureFoundryModel
metadata: { name: azure-gpt4o }
spec:
  endpoint: https://myfoundry.models.ai.azure.com
  deployment: gpt-4o
  auth: apiKey
```

### Notes

- `auth: entraId` → `DefaultAzureCredential` (`@azure/identity`).
- `auth: apiKey` → défaut `AZURE_OPENAI_API_KEY`.

## `McpStdio`

> Source d'outils MCP via transport stdio ; connexion persistante.

- **apiVersion:** `agent/v1`
- **runtime surface:** Permanent (connexion persistante)

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `command` | string | yes | — | Commande exécutée pour le transport stdio. |
| `args` | string[] | no (default) | `[]` | Arguments passés à la commande. |
| `cwd` | string | no | — | Répertoire de travail du sous-processus. |

### Example

```yaml
apiVersion: agent/v1
kind: McpStdio
metadata:
  name: recipe_db
  labels: { app: recipes }
spec:
  command: npx
  args: ["-y", "@example/recipe-mcp-server"]
  cwd: .
```

### Notes

- Connexion établie au load (fail-fast si le daemon ne répond pas).
- Tools publiés préfixés : `<name>__<tool>`.

## `Memory`

> Tooling de mémorisation volatile par AgentContext.

- **apiVersion:** `agent/v1`
- **runtime surface:** Permanent (sélectionné via `tools`)

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

_(no fields)_

### Example

```yaml
apiVersion: agent/v1
kind: Memory
metadata:
  name: pantry
spec: {}
```

### Notes

- Le `spec` est vide en v1 — l'état découle des appels au runtime.
- Tools publiés : `<name>__set` et `<name>__get`.
- Inerte pour la surface LLM tant qu'aucune entrée `toolset` ne le sélectionne.

## `OllamaModel`

> Service de complétion via un daemon Ollama (OpenAI-compatible).

- **apiVersion:** `agent/v1`
- **runtime surface:** Aucune (service pur)

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `description` | string | no | — | Description humaine (intent exposé au LLM pour les skills). |
| `model` | string | yes | — | Nom du modèle Ollama (requis). |
| `baseURL` | string | no | — | URL de base (défaut `OLLAMA_BASE_URL` ou `http://localhost:11434/v1`). |
| `apiKey` | string | no | — | Bearer optionnel pour un daemon distant (défaut `OLLAMA_API_KEY`). |

### Example

```yaml
apiVersion: agent/v1
kind: OllamaModel
metadata: { name: local }
spec:
  model: llama3.1
```

### Notes

- Local-first ; `OLLAMA_API_KEY` optionnel pour un daemon distant.
- `baseURL` défaut : `http://localhost:11434/v1`.

## `OpenAIModel`

> Service de complétion OpenAI (clé inline ou `OPENAI_*`).

- **apiVersion:** `agent/v1`
- **runtime surface:** Aucune (service pur)

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `description` | string | no | — | Description humaine (intent exposé au LLM pour les skills). |
| `model` | string | no | — | Nom du modèle OpenAI (défaut `OPENAI_MODEL`). |
| `baseURL` | string | no | — | URL de base (défaut `OPENAI_BASE_URL`). |
| `apiKey` | string | no | — | Clé API (défaut `OPENAI_API_KEY`). |
| `skillPlacement` | enum: `"system"` \| `"user"` | no | — | Placement des fragments SkillAttach (`system` ou `user`). |

### Example

```yaml
apiVersion: agent/v1
kind: OpenAIModel
metadata: { name: model-default }
spec:
  model: gpt-4o-mini
```

### Notes

- Auth via `OPENAI_API_KEY` (env) ou `spec.apiKey`.

## `Posture`

> État actif avec instruction + tooling + hooks ; activable via une route.

- **apiVersion:** `agent/v1`
- **runtime surface:** Active uniquement

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `extends` | string[] | no | — | Presets fusionnés au load (mécanique kind-spécifique). |
| `instruction` | union: object \| object \| string | yes | — | Instruction (`{ $ref }`, `{ content }`, ou chemin de fichier). |
| `tooling` | union: object \| object \| object[] | no (default) | `[]` | Surface d'outils — entries `toolset` / `route` / `subagent`. |
| `hooks` | object | no | — | Automations par trigger (`on_start` / `on_completion` / `on_tool_use` / `on_tool_error`). |
| `guardrails` | object[] | no | — | Assertions appliquées sur les tool calls. |

### Example

```yaml
apiVersion: agent/v1
kind: Posture
metadata:
  name: plan_meal
spec:
  extends: [kitchen_base]
  instruction: { $ref: ./instructions/plan-meal.md }
  tooling:
    - type: toolset
      tools: pantry/*
```

### Notes

- Inactive, une posture ne contribue rien à la surface LLM.
- Les transitions inter-postures passent par une entrée `type: route` explicite.

## `Preset`

> Conteneur de mutualisation ; inert tant qu'il n'est pas étendu via `extends`.

- **apiVersion:** `agent/v1`
- **runtime surface:** Aucune (inert hors `extends`)

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `instruction` | union: object \| object \| string | no | — | Instruction (`{ $ref }`, `{ content }`, ou chemin de fichier). |
| `tooling` | union: object \| object \| object[] | no (default) | `[]` | Surface d'outils — entries `toolset` / `route` / `subagent`. |
| `hooks` | object | no | — | Automations par trigger (`on_start` / `on_completion` / `on_tool_use` / `on_tool_error`). |
| `guardrails` | object[] | no | — | Assertions appliquées sur les tool calls. |

### Example

```yaml
apiVersion: agent/v1
kind: Preset
metadata:
  name: kitchen_base
spec:
  instruction: { content: "Be warm, concise and practical." }
  hooks:
    on_completion:
      - type: tooluse
        tool: interact__message
```

### Notes

- Un `Preset` n'accepte pas `extends` (réservé v1).
- Aucune surface runtime tant qu'il n'est pas étendu via `extends`.

## `Skill`

> Bundle toggable (instruction + tooling + hooks) attaché au runtime.

- **apiVersion:** `agent/v1`
- **runtime surface:** Attachée uniquement

### `metadata` _(commun à tous les kinds)_

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes | — | Identifiant unique de la ressource dans le blueprint. |
| `labels` | map<string, string> | no | — | Labels sélectionnables via `toolset.selector.matchLabels`. |
| `annotations` | map<string, string> | no | — | Annotations libres (non-sélectionnables). |

### `spec`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `extends` | string[] | no | — | Presets fusionnés au load (mécanique kind-spécifique). |
| `description` | string | no | — | Description humaine (intent exposé au LLM pour les skills). |
| `instruction` | union: object \| object \| string | no | — | Instruction (`{ $ref }`, `{ content }`, ou chemin de fichier). |
| `tooling` | union: object \| object \| object[] | no (default) | `[]` | Surface d'outils — entries `toolset` / `route` / `subagent`. |
| `hooks` | object | no | — | Automations par trigger (`on_start` / `on_completion` / `on_tool_use` / `on_tool_error`). |
| `guardrails` | object[] | no | — | Assertions appliquées sur les tool calls. |

### Example

```yaml
apiVersion: agent/v1
kind: Skill
metadata:
  name: substitutions
spec:
  description: Suggest ingredient substitutions for dietary needs.
  instruction: { $ref: ./instructions/substitutions.md }
  tooling:
    - type: toolset
      tools: pantry/*
```

### Notes

- Activation via une entrée `toolset tools: <name>/*` dans une posture ou skill.
- Désactivation automatique au changement de posture ou à la fermeture du context.

